#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strlen - returns the length of a string
 * @s: the string to be computed
 * Return: the length of s
 */

int _strlen(char *s)
{
	return (strlen(s));
}
