#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _isalpha - checks for alphabets
 * @c: letter to be checked
 * Return: the letter
 */

int _isalpha(int c)
{
	return (isalpha(c));
}
