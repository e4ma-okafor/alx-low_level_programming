#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _isdigit - checks for digits
 * @c: the number to be checked
 * Return: c
 */

int _isdigit(int c)
{
	return (isdigit(c));
}
