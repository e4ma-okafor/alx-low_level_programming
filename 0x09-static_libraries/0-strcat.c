#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strcat - Concatenates two strings
 * @src: string 1
 * @dest: string 2
 * Return: the result
 */

char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
