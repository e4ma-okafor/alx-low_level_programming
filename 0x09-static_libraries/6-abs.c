#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _abs - checks absolute value of n
 * @n: the number to be checked
 * Return: the number
 */

int _abs(int n)
{
	return (abs(n));
}
