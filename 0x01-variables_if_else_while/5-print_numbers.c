#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 */

int main(void)

{
	int dit;

	for (dit = 0; dit < 10; dit++)
	{
		printf("%d", dit);
	}
	printf("\n");

	return (0);
}
